# Madlenianum Angular client

See the repository root `README.md` for supported Node versions, environment configuration, seed order, upload setup, local admin credentials, and Media Library instructions.

```powershell
cd C:\Zepter\Madlenianum\client
npm install
npm start
```

Production build:

```powershell
npm run build
```
